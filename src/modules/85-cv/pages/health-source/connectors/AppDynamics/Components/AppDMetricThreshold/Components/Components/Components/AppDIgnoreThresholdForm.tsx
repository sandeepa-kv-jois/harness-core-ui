import React from 'react'
import { Container, Layout, Text, ButtonVariation, FormInput } from '@harness/uicore'

export default function AppDIgnoreThresholdForm(props) {
  const {
    form: {
      values: { ignoreThresholds }
    }
  } = props
  return <Container></Container>
}
